function [ output_args ] = GetPosteriorMode( input_args )
% Uses a numerical optimizer to find the peak of the posterior density for
% the Bayesian VAR model summarized by a posterior estimate (postest)
% structure containing draws from the posterior density (see GibbsBVAR.m).


end

